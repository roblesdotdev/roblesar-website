---
interface Props {
  date: Date
}

const { date } = Astro.props
---

<time class="text-fg-muted text-sm italic" datetime={date.toISOString()}>
  {
    date.toLocaleDateString('en-us', {
      timeZone: 'UTC',
      year: 'numeric',
      month: 'short',
      day: 'numeric',
    })
  }
</time>
